# 06 适配器与集成策略（微信/支付宝）

- 端口-适配器 + 防腐层：统一 `PaymentGatewayPort` 接口，不泄露平台差异到应用/领域层。
- 签名与证书：
  - 微信 v3：平台证书自动更新，时间戳/随机串校验，回调原文验签。
  - 支付宝 RSA2：开放平台公钥管理，回调参数排序 + 签名串计算。
- 回调：
  - 保留原文（raw body），严格验签；事件唯一键去重；失败重试与幂等。
- 错误与安全：
  - 最小权限的证书/密钥访问；日志脱敏；防重放（时间窗+nonce）。