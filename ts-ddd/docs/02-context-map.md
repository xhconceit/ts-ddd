# 02 领域划分与上下文映射

- 支付（Payment BC）：创建/确认/取消/退款，维护内部交易状态。
- 回调（Webhook BC）：接收微信/支付宝回调，验签与去重，向应用层发布事件。
- 对账（Reconciliation BC）：拉取平台账单，匹配内部流水，生成差异与报表。

上下文关系
- 回调 → 支付：事件驱动（已支付/退款成功等）
- 对账 ↔ 支付：按日批处理 + 读模型查询

一致性边界
- 聚合根 `Payment` 保证内部不变式；跨上下文通过 Outbox + MQ 实现最终一致。